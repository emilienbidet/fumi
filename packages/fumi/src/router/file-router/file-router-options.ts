/**
 * Configuration options for the FileRouter
 */
export interface FileRouterOptions {
	/**
	 * The directory path where route files are located
	 * @default "./routes"
	 */
	routesDirectory: string;
}
